{
  "skill_name": "pre-pr-audit",
  "evals": [
    {
      "id": 1,
      "name": "broadcast-without-destroy",
      "prompt": "I'm about to create a PR. Can you check my code for quality issues?\n\nChanged file: FullExportJob.java\nAdded broadcast variable without cleanup in finally block.",
      "expected_output": "Should detect .broadcast() without .destroy() and offer to add cleanup in finally block",
      "files": [],
      "assertions": []
    },
    {
      "id": 2,
      "name": "duplicate-key-validation",
      "prompt": "Ready to push my changes. Please run a quality check.\n\nChanged file: MetadataProcessor.java\nUsing Collectors.toMap() without checking for duplicate keys.",
      "expected_output": "Should detect Collectors.toMap() without duplicate validation and offer to add pre-check or merge function",
      "files": [],
      "assertions": []
    },
    {
      "id": 3,
      "name": "silent-failure-warning",
      "prompt": "Before I create the PR, check for issues.\n\nChanged file: DataValidator.java\nLogging warnings for duplicate records instead of failing fast.",
      "expected_output": "Should detect LOGGER.warn with data quality keywords and suggest throwing exception instead",
      "files": [],
      "assertions": []
    },
    {
      "id": 4,
      "name": "missing-test-coverage",
      "prompt": "Quality check before PR creation.\n\nChanged files: RecordProcessor.java (new class), no test file created.",
      "expected_output": "Should detect new class without corresponding test file and offer to create test structure",
      "files": [],
      "assertions": []
    },
    {
      "id": 5,
      "name": "map-get-without-null-check",
      "prompt": "I'm about to create a PR. Can you check my code for quality issues?\n\nChanged file: ShardResolver.java\nCalling metadataMap.get(shardKey) and using the result directly, with no getOrDefault(), Optional wrapping, or null check nearby.",
      "expected_output": "Should detect .get() on a Map used without a nearby null check, getOrDefault(), or Optional.ofNullable(), and recommend one of those instead of using the result directly",
      "files": [],
      "assertions": []
    },
    {
      "id": 6,
      "name": "try-finally-scope-gap",
      "prompt": "Ready to push my changes. Please run a quality check.\n\nChanged file: PartitionJob.java\nCalls rdd.persist(), then runs a validation step, then opens a try block whose finally calls rdd.unpersist().",
      "expected_output": "Should detect operations between the persist()/broadcast() call and the try block, and recommend moving those operations inside the try block so the finally-block cleanup still runs if they throw",
      "files": [],
      "assertions": []
    },
    {
      "id": 7,
      "name": "list-add-without-dedup",
      "prompt": "Before I create the PR, check for issues.\n\nChanged file: RecordAggregator.java\nInside a for loop, calls resultList.add(record) with no Set, .contains() check, or visited-tracking anywhere nearby.",
      "expected_output": "Should detect list.add() inside a loop with no deduplication guard (Set, .contains(), or visited tracking) and recommend switching to a Set or adding a contains() check",
      "files": [],
      "assertions": []
    },
    {
      "id": 8,
      "name": "tostring-equals-polymorphic",
      "prompt": "Quality check before PR creation.\n\nChanged file: ExpressionComparator.java\nCompares two Expression nodes with node.toString().equals(other.toString()) instead of a type-safe check.",
      "expected_output": "Should detect .toString().equals() on a potentially polymorphic receiver and recommend instanceof, a type discriminator like .getKind(), or an explicit type-safe equality method instead",
      "files": [],
      "assertions": []
    },
    {
      "id": 9,
      "name": "string-shape-type-proxy",
      "prompt": "I'm about to create a PR. Can you check my code for quality issues?\n\nChanged file: TokenClassifier.java\nUses token.toString().startsWith(\"\\\"\") to decide whether a token is a string literal.",
      "expected_output": "Should detect startsWith/endsWith on toString() used as a type discriminator and recommend a proper type check (instanceof, .getKind(), or an enum discriminator) instead of string-shape matching",
      "files": [],
      "assertions": []
    },
    {
      "id": 10,
      "name": "parallel-derivation-constant",
      "prompt": "Ready to push my changes. Please run a quality check.\n\nChanged files: Encoder.java (hardcodes the literal 37 as a truncation length) and Decoder.java (independently hardcodes the same literal 37 as the expected length).",
      "expected_output": "Should detect the same non-common numeric constant hardcoded independently across two changed files and recommend extracting it to one shared constant both files import",
      "files": [],
      "assertions": []
    },
    {
      "id": 11,
      "name": "narrow-catch-library-boundary",
      "prompt": "Before I create the PR, check for issues.\n\nChanged file: ConfigLoader.java\nCalls a java.nio.file API and catches NoSuchFileException specifically instead of a broader IOException.",
      "expected_output": "Should detect a narrow catch of a specific exception subtype around a JDK/library call and recommend catching the broader documented type (or multiple sibling types), since the API contract may not guarantee only that subtype",
      "files": [],
      "assertions": []
    },
    {
      "id": 12,
      "name": "subprocess-missing-safety-guards",
      "prompt": "Quality check before PR creation.\n\nChanged file: build_runner.py\nCalls subprocess.run(['mvn', 'test']) with no cwd=, no timeout=, no except subprocess.TimeoutExpired handler, and no shutil.which()/os.path.exists() guard beforehand.",
      "expected_output": "Should detect the subprocess.run() call missing cwd=, timeout=, a TimeoutExpired handler, and an executable-existence guard, and recommend adding each",
      "files": [],
      "assertions": []
    }
  ]
}
