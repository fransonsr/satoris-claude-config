{
  "_comment": "Declarative hook configuration for this plugin. install.sh reads this to register the hook in ~/.claude/settings.json.",
  "hooks": [
    {
      "event": "PreCompact",
      "matcher": "auto",
      "command": "precompact-checkpoint-reminder.sh",
      "description": "Block auto-compaction and remind the user to run /handoff:continue before proceeding. Explicit /compact is always the override."
    }
  ]
}
